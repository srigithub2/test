name: CodeQL – Vehicle Standards

on:
  workflow_call:

jobs:
  scan:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        language: [cpp, c]
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v4

      - uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          packs: ./vehicle

      - uses: github/codeql-action/analyze@v3
      
