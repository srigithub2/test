name: "CodeQL – C & C++ Coding-Standards Only (Optimized)"

on:
  pull_request:
    branches: [ "main", "master" ]
    types: [ opened, synchronize, reopened ]
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  coding-standards:
    name: "Coding-Standards (CERT, AUTOSAR, MISRA)"
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      # ───────── 1. Checkout source ─────────
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          path: pr-code
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      # ───────── 2. Cache & setup ccache ─────────
      - name: Cache build objects
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/ccache
            pr-code/**/*.o
          key: ${{ runner.os }}-build-${{ hashFiles('pr-code/**/*.c','pr-code/**/*.cpp','pr-code/**/*.h','pr-code/**/*.hpp') }}
          restore-keys: ${{ runner.os }}-build-

      - name: Setup ccache
        run: |
          sudo apt-get update -y && sudo apt-get install -y ccache
          echo "CC='ccache gcc'"  >> $GITHUB_ENV
          echo "CXX='ccache g++'" >> $GITHUB_ENV
          ccache --set-config=max_size=1G
          ccache --zero-stats

      # ───────── 3. Write CodeQL config ─────────
      - name: Write CodeQL config
        run: |
          mkdir -p pr-code/.github
          {
            echo "paths-ignore:"
            echo "  - third_party/**"
            echo "  - generated/**"
            echo "  - vendor/**"
            echo "  - build/**"
            echo "  - '**/test/**'"
          } > pr-code/.github/codeql-config.yml

      # ───────── 4. Init CodeQL with pre-compiled v2.47.0 pack ─────────
      - name: Init CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: c-cpp
          config-file: pr-code/.github/codeql-config.yml
          packs: github/codeql-coding-standards@v2.47.0
      # This downloads the v2.47.0 pack and its QL bytecode—no source compile needed :contentReference[oaicite:0]{index=0}:contentReference[oaicite:1]{index=1}

      # ───────── 5. Create DB (skip any C/C++ compile) ─────────
      - name: Create CodeQL database
        working-directory: pr-code
        run: |
          codeql database create coding-standards-db \
            --language=c-cpp \
            --build-mode=none \
            --overwrite

      # ───────── 6. Analyze (uses the packs from init) ─────────
      - name: Analyze coding-standards packs
        uses: github/codeql-action/analyze@v3
        with:
          checkout_path: pr-code
          category: coding-standards
          output: cert-results.sarif
          threads: ${{ runner.processor_count }}
          upload: false

      # ───────── 7. Upload results ─────────
      - name: Upload SARIF to Security Tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: pr-code/cert-results.sarif
          category: coding-standards

      - name: Upload SARIF artifact
        uses: actions/upload-artifact@v4
        with:
          name: coding-standards-sarif
          path: pr-code/cert-results.sarif

      - name: Show ccache stats
        if: always()
        run: ccache --show-stats || true
